# UI

Authenticated screens use the Sisahygo Connect application shell. Blade components are presentation-only. Livewire components should retrieve authorized data through domain services and query objects.