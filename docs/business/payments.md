# Payments

Payment visibility is independent from shipment visibility. Receiver payment access requires authorized receiver linkage, `can_view_payment`, `payment.view`, and `paymenttype IN ('E', 'L')`.