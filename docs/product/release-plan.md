# Release Plan

Sprint 1 establishes the account, authorization, data isolation, and documentation foundation. Later releases must build on the domain query and policy architecture instead of bypassing it.